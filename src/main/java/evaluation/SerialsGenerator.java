package evaluation;

/**
 * Created by Anthony on 17/11/2016.
 * Package : evaluation .
 * Project : PhDTrack.
 */
public class SerialsGenerator {

    public static void main (String[] args) {

		/*
		Corpus corpus = Data_loading.load_data(Constants_Global.DBNAME, Constants_Global.DBCOLLECTIONTWITTERFDL2015, -1);
	    List<String> lines = corpus.get_texts();

	    // -- Clean the big corpus with the same parameters
	    lines = Data_processing.cleanBigCorpus(lines, Arrays.asList(10, 9));
	    corpus.set_texts(lines);
	    for (int i =0; i < 10; i++){
	        System.out.println(corpus.get_texts().get(i));
	    }
	    Toolbox.serialization(corpus, "bigcorpus.serial");
	    */
    }
}
